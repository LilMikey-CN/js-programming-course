function addFive(val) {
    return val + 5;
}

module.exports = addFive;

// Then in command line input: npm init -y , to init a package.json and 
// return yes to every question

// Then input: npm install --save-dev jest 
// --save-dev means to save modules locally/in this project. 
// Comparing to -g option which save modules globally.
